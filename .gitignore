# Rust
/target
**/*.rs.bk
*.pdb

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
.claude/
# graphify knowledge graph (local, not committed)
graphify-out/

# Local Cargo override — redirects backbone-inventory/backbone-accounting git dev-deps to ../backbone-*
# in dev. .cargo/config.toml.example is tracked as the template; the real file stays local.
.cargo/config.toml
.cargo/config.toml.quarantined
