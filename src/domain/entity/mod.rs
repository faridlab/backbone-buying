//! Domain Entities
//!
//! Generated by metaphor-schema. Do not edit manually.

pub mod material_request;
pub mod material_request_item;
pub mod purchase_agreement;
pub mod purchase_agreement_line;
pub mod purchase_company_setting;
pub mod purchase_order;
pub mod purchase_order_item;
pub mod request_for_quotation;
pub mod rfq_item;
pub mod rfq_supplier;
pub mod supplier_price;
pub mod supplier_quotation;
pub mod supplier_quotation_item;
pub mod supplier_reminder_setting;
pub mod material_request_type;
pub mod purchase_agreement_status;
pub mod agreement_kind;
pub mod double_validation;
pub mod purchase_order_status;
pub mod purchase_receipt_status;
pub mod purchase_invoice_status;
pub mod qty_received_method;
pub mod purchase_method;
pub mod order_kind;
pub mod purchase_doc_status;

// Re-exports
pub use material_request::MaterialRequest;
pub use material_request::MaterialRequestBuilder;
pub use material_request::MaterialRequestId;
pub use material_request_item::MaterialRequestItem;
pub use material_request_item::MaterialRequestItemBuilder;
pub use material_request_item::MaterialRequestItemId;
pub use purchase_agreement::PurchaseAgreement;
pub use purchase_agreement::PurchaseAgreementBuilder;
pub use purchase_agreement::PurchaseAgreementId;
pub use purchase_agreement_line::PurchaseAgreementLine;
pub use purchase_agreement_line::PurchaseAgreementLineBuilder;
pub use purchase_agreement_line::PurchaseAgreementLineId;
pub use purchase_company_setting::PurchaseCompanySetting;
pub use purchase_company_setting::PurchaseCompanySettingBuilder;
pub use purchase_company_setting::PurchaseCompanySettingId;
pub use purchase_order::PurchaseOrder;
pub use purchase_order::PurchaseOrderBuilder;
pub use purchase_order::PurchaseOrderId;
pub use purchase_order_item::PurchaseOrderItem;
pub use purchase_order_item::PurchaseOrderItemBuilder;
pub use purchase_order_item::PurchaseOrderItemId;
pub use request_for_quotation::RequestForQuotation;
pub use request_for_quotation::RequestForQuotationBuilder;
pub use request_for_quotation::RequestForQuotationId;
pub use rfq_item::RfqItem;
pub use rfq_item::RfqItemBuilder;
pub use rfq_item::RfqItemId;
pub use rfq_supplier::RfqSupplier;
pub use rfq_supplier::RfqSupplierBuilder;
pub use rfq_supplier::RfqSupplierId;
pub use supplier_price::SupplierPrice;
pub use supplier_price::SupplierPriceBuilder;
pub use supplier_price::SupplierPriceId;
pub use supplier_quotation::SupplierQuotation;
pub use supplier_quotation::SupplierQuotationBuilder;
pub use supplier_quotation::SupplierQuotationId;
pub use supplier_quotation_item::SupplierQuotationItem;
pub use supplier_quotation_item::SupplierQuotationItemBuilder;
pub use supplier_quotation_item::SupplierQuotationItemId;
pub use supplier_reminder_setting::SupplierReminderSetting;
pub use supplier_reminder_setting::SupplierReminderSettingBuilder;
pub use supplier_reminder_setting::SupplierReminderSettingId;
pub use material_request_type::MaterialRequestType;
pub use purchase_agreement_status::PurchaseAgreementStatus;
pub use agreement_kind::AgreementKind;
pub use double_validation::DoubleValidation;
pub use purchase_order_status::PurchaseOrderStatus;
pub use purchase_receipt_status::PurchaseReceiptStatus;
pub use purchase_invoice_status::PurchaseInvoiceStatus;
pub use qty_received_method::QtyReceivedMethod;
pub use purchase_method::PurchaseMethod;
pub use order_kind::OrderKind;
pub use purchase_doc_status::PurchaseDocStatus;

// ==========================================================================
// Entity Trait
// ==========================================================================

use std::fmt::Debug;

/// Trait for domain entities
///
/// All generated entities implement this trait, providing
/// a common interface for working with domain objects.
pub trait Entity: Debug + Clone {
    /// The type of the entity's unique identifier
    type Id: Debug + Clone + PartialEq;

    /// Get the entity's unique identifier
    fn entity_id(&self) -> &Self::Id;

    /// Get the entity type name
    fn entity_type() -> &'static str;
}

// ==========================================================================
// Audit Metadata
// ==========================================================================

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use uuid::Uuid;
#[cfg(feature = "openapi")]
use utoipa::ToSchema;

/// Audit metadata stored as JSONB in the database
///
/// This struct consolidates audit fields (timestamps and actors) into a single
/// JSONB column for efficient storage and flexible querying.
#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq)]
#[cfg_attr(feature = "openapi", derive(ToSchema))]
pub struct AuditMetadata {
    /// Timestamp when the record was created
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was last updated
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was soft-deleted
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_at: Option<DateTime<Utc>>,

    /// User ID who created the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_by: Option<Uuid>,

    /// User ID who last updated the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<Uuid>,

    /// User ID who deleted the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_by: Option<Uuid>,
}

impl AuditMetadata {
    /// Create new audit metadata with created_at set to now
    pub fn new() -> Self {
        Self {
            created_at: Some(Utc::now()),
            ..Default::default()
        }
    }

    /// Create with creator ID
    pub fn with_creator(creator_id: Uuid) -> Self {
        Self {
            created_at: Some(Utc::now()),
            created_by: Some(creator_id),
            ..Default::default()
        }
    }

    /// Touch the record (update updated_at)
    pub fn touch(&mut self) {
        self.updated_at = Some(Utc::now());
    }

    /// Touch with updater ID
    pub fn touch_by(&mut self, updater_id: Uuid) {
        self.updated_at = Some(Utc::now());
        self.updated_by = Some(updater_id);
    }

    /// Soft delete the record
    pub fn soft_delete(&mut self) {
        self.deleted_at = Some(Utc::now());
    }

    /// Soft delete with deleter ID
    pub fn soft_delete_by(&mut self, deleter_id: Uuid) {
        self.deleted_at = Some(Utc::now());
        self.deleted_by = Some(deleter_id);
    }

    /// Restore a soft-deleted record
    pub fn restore(&mut self) {
        self.deleted_at = None;
        self.deleted_by = None;
    }

    /// Check if record is deleted
    pub fn is_deleted(&self) -> bool {
        self.deleted_at.is_some()
    }

    /// Check if record is active (not deleted)
    pub fn is_active(&self) -> bool {
        self.deleted_at.is_none()
    }
}
