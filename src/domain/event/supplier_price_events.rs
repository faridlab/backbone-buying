//! Domain events for SupplierPrice entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::SupplierPrice;

/// Domain events for SupplierPrice — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<SupplierPrice>)` variant.
pub type SupplierPriceEvent = CrudEvent<SupplierPrice>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for SupplierPrice events.
pub type SupplierPriceEventPublisher = Arc<dyn CrudEventPublisher<SupplierPrice> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSupplierPriceEventPublisher = NoOpCrudEventPublisher<SupplierPrice>;
