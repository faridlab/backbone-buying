//! Domain events for RequestForQuotation entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::RequestForQuotation;

/// Domain events for RequestForQuotation — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<RequestForQuotation>)` variant.
pub type RequestForQuotationEvent = CrudEvent<RequestForQuotation>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for RequestForQuotation events.
pub type RequestForQuotationEventPublisher = Arc<dyn CrudEventPublisher<RequestForQuotation> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRequestForQuotationEventPublisher = NoOpCrudEventPublisher<RequestForQuotation>;
