//! Repository trait for MaterialRequestItem aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MaterialRequestItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MaterialRequestItem;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MaterialRequestItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MaterialRequestItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MaterialRequestItemPaginatedResult {
    pub data: Vec<MaterialRequestItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MaterialRequestItemFilter {
    pub request_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
}

impl MaterialRequestItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.request_id.is_some() || self.company_id.is_some() || self.item_id.is_some()
    }
}

/// Repository trait for MaterialRequestItem aggregate
///
/// This trait defines the persistence contract for MaterialRequestItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MaterialRequestItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new material_request_item entity
    async fn save(&self, entity: &MaterialRequestItem) -> Result<MaterialRequestItem>;

    /// Find material_request_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MaterialRequestItem>>;

    /// Find all material_request_item entities
    async fn find_all(&self) -> Result<Vec<MaterialRequestItem>>;

    /// Update material_request_item by ID
    async fn update(&self, id: &str, entity: &MaterialRequestItem) -> Result<Option<MaterialRequestItem>>;

    /// Delete material_request_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List material_request_item with pagination
    async fn list(&self, params: MaterialRequestItemPaginationParams) -> Result<MaterialRequestItemPaginatedResult>;

    /// List material_request_item with pagination and filters
    async fn list_with_filters(&self, params: MaterialRequestItemPaginationParams, filters: MaterialRequestItemFilter) -> Result<MaterialRequestItemPaginatedResult>;

    /// Count all material_request_item entities
    async fn count(&self) -> Result<u64>;

    /// Count material_request_item entities matching filters
    async fn count_with_filters(&self, filters: MaterialRequestItemFilter) -> Result<u64>;

    /// Check if material_request_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete material_request_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted material_request_item by ID
    async fn restore(&self, id: &str) -> Result<Option<MaterialRequestItem>>;

    /// List soft-deleted material_request_item entities
    async fn list_deleted(&self, params: MaterialRequestItemPaginationParams) -> Result<MaterialRequestItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save material_request_item entities
    async fn bulk_save(&self, entities: &[MaterialRequestItem]) -> Result<Vec<MaterialRequestItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
