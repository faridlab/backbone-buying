//! Repository trait for SupplierQuotationItem aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SupplierQuotationItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::SupplierQuotationItem;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SupplierQuotationItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SupplierQuotationItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SupplierQuotationItemPaginatedResult {
    pub data: Vec<SupplierQuotationItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SupplierQuotationItemFilter {
    pub quotation_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
}

impl SupplierQuotationItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.quotation_id.is_some() || self.item_id.is_some()
    }
}

/// Repository trait for SupplierQuotationItem aggregate
///
/// This trait defines the persistence contract for SupplierQuotationItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SupplierQuotationItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new supplier_quotation_item entity
    async fn save(&self, entity: &SupplierQuotationItem) -> Result<SupplierQuotationItem>;

    /// Find supplier_quotation_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SupplierQuotationItem>>;

    /// Find all supplier_quotation_item entities
    async fn find_all(&self) -> Result<Vec<SupplierQuotationItem>>;

    /// Update supplier_quotation_item by ID
    async fn update(&self, id: &str, entity: &SupplierQuotationItem) -> Result<Option<SupplierQuotationItem>>;

    /// Delete supplier_quotation_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List supplier_quotation_item with pagination
    async fn list(&self, params: SupplierQuotationItemPaginationParams) -> Result<SupplierQuotationItemPaginatedResult>;

    /// List supplier_quotation_item with pagination and filters
    async fn list_with_filters(&self, params: SupplierQuotationItemPaginationParams, filters: SupplierQuotationItemFilter) -> Result<SupplierQuotationItemPaginatedResult>;

    /// Count all supplier_quotation_item entities
    async fn count(&self) -> Result<u64>;

    /// Count supplier_quotation_item entities matching filters
    async fn count_with_filters(&self, filters: SupplierQuotationItemFilter) -> Result<u64>;

    /// Check if supplier_quotation_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete supplier_quotation_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted supplier_quotation_item by ID
    async fn restore(&self, id: &str) -> Result<Option<SupplierQuotationItem>>;

    /// List soft-deleted supplier_quotation_item entities
    async fn list_deleted(&self, params: SupplierQuotationItemPaginationParams) -> Result<SupplierQuotationItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save supplier_quotation_item entities
    async fn bulk_save(&self, entities: &[SupplierQuotationItem]) -> Result<Vec<SupplierQuotationItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
