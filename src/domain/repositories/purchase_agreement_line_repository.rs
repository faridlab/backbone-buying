//! Repository trait for PurchaseAgreementLine aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PurchaseAgreementLine aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::PurchaseAgreementLine;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PurchaseAgreementLinePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PurchaseAgreementLinePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PurchaseAgreementLinePaginatedResult {
    pub data: Vec<PurchaseAgreementLine>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PurchaseAgreementLineFilter {
    pub agreement_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
}

impl PurchaseAgreementLineFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.agreement_id.is_some() || self.company_id.is_some() || self.item_id.is_some()
    }
}

/// Repository trait for PurchaseAgreementLine aggregate
///
/// This trait defines the persistence contract for PurchaseAgreementLine entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PurchaseAgreementLineRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new purchase_agreement_line entity
    async fn save(&self, entity: &PurchaseAgreementLine) -> Result<PurchaseAgreementLine>;

    /// Find purchase_agreement_line by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PurchaseAgreementLine>>;

    /// Find all purchase_agreement_line entities
    async fn find_all(&self) -> Result<Vec<PurchaseAgreementLine>>;

    /// Update purchase_agreement_line by ID
    async fn update(&self, id: &str, entity: &PurchaseAgreementLine) -> Result<Option<PurchaseAgreementLine>>;

    /// Delete purchase_agreement_line by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List purchase_agreement_line with pagination
    async fn list(&self, params: PurchaseAgreementLinePaginationParams) -> Result<PurchaseAgreementLinePaginatedResult>;

    /// List purchase_agreement_line with pagination and filters
    async fn list_with_filters(&self, params: PurchaseAgreementLinePaginationParams, filters: PurchaseAgreementLineFilter) -> Result<PurchaseAgreementLinePaginatedResult>;

    /// Count all purchase_agreement_line entities
    async fn count(&self) -> Result<u64>;

    /// Count purchase_agreement_line entities matching filters
    async fn count_with_filters(&self, filters: PurchaseAgreementLineFilter) -> Result<u64>;

    /// Check if purchase_agreement_line exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete purchase_agreement_line by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted purchase_agreement_line by ID
    async fn restore(&self, id: &str) -> Result<Option<PurchaseAgreementLine>>;

    /// List soft-deleted purchase_agreement_line entities
    async fn list_deleted(&self, params: PurchaseAgreementLinePaginationParams) -> Result<PurchaseAgreementLinePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save purchase_agreement_line entities
    async fn bulk_save(&self, entities: &[PurchaseAgreementLine]) -> Result<Vec<PurchaseAgreementLine>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
