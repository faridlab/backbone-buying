//! Repository trait for SupplierQuotation aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SupplierQuotation aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{SupplierQuotation, PurchaseDocStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SupplierQuotationPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SupplierQuotationPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SupplierQuotationPaginatedResult {
    pub data: Vec<SupplierQuotation>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SupplierQuotationFilter {
    pub quotation_number: Option<String>,
    pub rfq_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub supplier_id: Option<Uuid>,
    pub status: Option<PurchaseDocStatus>,
    pub currency: Option<String>,
    pub notes: Option<String>,
}

impl SupplierQuotationFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.quotation_number.is_some() || self.rfq_id.is_some() || self.company_id.is_some() || self.supplier_id.is_some() || self.status.is_some() || self.currency.is_some() || self.notes.is_some()
    }
}

/// Repository trait for SupplierQuotation aggregate
///
/// This trait defines the persistence contract for SupplierQuotation entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SupplierQuotationRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new supplier_quotation entity
    async fn save(&self, entity: &SupplierQuotation) -> Result<SupplierQuotation>;

    /// Find supplier_quotation by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SupplierQuotation>>;

    /// Find all supplier_quotation entities
    async fn find_all(&self) -> Result<Vec<SupplierQuotation>>;

    /// Update supplier_quotation by ID
    async fn update(&self, id: &str, entity: &SupplierQuotation) -> Result<Option<SupplierQuotation>>;

    /// Delete supplier_quotation by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List supplier_quotation with pagination
    async fn list(&self, params: SupplierQuotationPaginationParams) -> Result<SupplierQuotationPaginatedResult>;

    /// List supplier_quotation with pagination and filters
    async fn list_with_filters(&self, params: SupplierQuotationPaginationParams, filters: SupplierQuotationFilter) -> Result<SupplierQuotationPaginatedResult>;

    /// Count all supplier_quotation entities
    async fn count(&self) -> Result<u64>;

    /// Count supplier_quotation entities matching filters
    async fn count_with_filters(&self, filters: SupplierQuotationFilter) -> Result<u64>;

    /// Check if supplier_quotation exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete supplier_quotation by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted supplier_quotation by ID
    async fn restore(&self, id: &str) -> Result<Option<SupplierQuotation>>;

    /// List soft-deleted supplier_quotation entities
    async fn list_deleted(&self, params: SupplierQuotationPaginationParams) -> Result<SupplierQuotationPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save supplier_quotation entities
    async fn bulk_save(&self, entities: &[SupplierQuotation]) -> Result<Vec<SupplierQuotation>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
