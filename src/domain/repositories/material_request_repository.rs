//! Repository trait for MaterialRequest aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MaterialRequest aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{MaterialRequest, MaterialRequestType, PurchaseDocStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MaterialRequestPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MaterialRequestPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MaterialRequestPaginatedResult {
    pub data: Vec<MaterialRequest>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MaterialRequestFilter {
    pub request_number: Option<String>,
    pub company_id: Option<Uuid>,
    pub request_type: Option<MaterialRequestType>,
    pub status: Option<PurchaseDocStatus>,
    pub notes: Option<String>,
}

impl MaterialRequestFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.request_number.is_some() || self.company_id.is_some() || self.request_type.is_some() || self.status.is_some() || self.notes.is_some()
    }
}

/// Repository trait for MaterialRequest aggregate
///
/// This trait defines the persistence contract for MaterialRequest entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MaterialRequestRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new material_request entity
    async fn save(&self, entity: &MaterialRequest) -> Result<MaterialRequest>;

    /// Find material_request by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MaterialRequest>>;

    /// Find all material_request entities
    async fn find_all(&self) -> Result<Vec<MaterialRequest>>;

    /// Update material_request by ID
    async fn update(&self, id: &str, entity: &MaterialRequest) -> Result<Option<MaterialRequest>>;

    /// Delete material_request by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List material_request with pagination
    async fn list(&self, params: MaterialRequestPaginationParams) -> Result<MaterialRequestPaginatedResult>;

    /// List material_request with pagination and filters
    async fn list_with_filters(&self, params: MaterialRequestPaginationParams, filters: MaterialRequestFilter) -> Result<MaterialRequestPaginatedResult>;

    /// Count all material_request entities
    async fn count(&self) -> Result<u64>;

    /// Count material_request entities matching filters
    async fn count_with_filters(&self, filters: MaterialRequestFilter) -> Result<u64>;

    /// Check if material_request exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete material_request by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted material_request by ID
    async fn restore(&self, id: &str) -> Result<Option<MaterialRequest>>;

    /// List soft-deleted material_request entities
    async fn list_deleted(&self, params: MaterialRequestPaginationParams) -> Result<MaterialRequestPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save material_request entities
    async fn bulk_save(&self, entities: &[MaterialRequest]) -> Result<Vec<MaterialRequest>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
