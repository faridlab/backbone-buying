//! Repository trait for SupplierReminderSetting aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SupplierReminderSetting aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::SupplierReminderSetting;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SupplierReminderSettingPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SupplierReminderSettingPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SupplierReminderSettingPaginatedResult {
    pub data: Vec<SupplierReminderSetting>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SupplierReminderSettingFilter {
    pub company_id: Option<Uuid>,
    pub supplier_id: Option<Uuid>,
    pub receipt_reminder_email: Option<bool>,
}

impl SupplierReminderSettingFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.supplier_id.is_some() || self.receipt_reminder_email.is_some()
    }
}

/// Repository trait for SupplierReminderSetting aggregate
///
/// This trait defines the persistence contract for SupplierReminderSetting entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SupplierReminderSettingRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new supplier_reminder_setting entity
    async fn save(&self, entity: &SupplierReminderSetting) -> Result<SupplierReminderSetting>;

    /// Find supplier_reminder_setting by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SupplierReminderSetting>>;

    /// Find all supplier_reminder_setting entities
    async fn find_all(&self) -> Result<Vec<SupplierReminderSetting>>;

    /// Update supplier_reminder_setting by ID
    async fn update(&self, id: &str, entity: &SupplierReminderSetting) -> Result<Option<SupplierReminderSetting>>;

    /// Delete supplier_reminder_setting by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List supplier_reminder_setting with pagination
    async fn list(&self, params: SupplierReminderSettingPaginationParams) -> Result<SupplierReminderSettingPaginatedResult>;

    /// List supplier_reminder_setting with pagination and filters
    async fn list_with_filters(&self, params: SupplierReminderSettingPaginationParams, filters: SupplierReminderSettingFilter) -> Result<SupplierReminderSettingPaginatedResult>;

    /// Count all supplier_reminder_setting entities
    async fn count(&self) -> Result<u64>;

    /// Count supplier_reminder_setting entities matching filters
    async fn count_with_filters(&self, filters: SupplierReminderSettingFilter) -> Result<u64>;

    /// Check if supplier_reminder_setting exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete supplier_reminder_setting by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted supplier_reminder_setting by ID
    async fn restore(&self, id: &str) -> Result<Option<SupplierReminderSetting>>;

    /// List soft-deleted supplier_reminder_setting entities
    async fn list_deleted(&self, params: SupplierReminderSettingPaginationParams) -> Result<SupplierReminderSettingPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save supplier_reminder_setting entities
    async fn bulk_save(&self, entities: &[SupplierReminderSetting]) -> Result<Vec<SupplierReminderSetting>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
