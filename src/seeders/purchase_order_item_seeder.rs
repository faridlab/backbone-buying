//! PurchaseOrderItem seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial PurchaseOrderItem data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PURCHASEORDERITEM SEEDER
// ============================================================================

/// Seeder for purchase_order_item
pub struct SeedPurchaseOrderItemSeeder;

impl SeedPurchaseOrderItemSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPurchaseOrderItemSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPurchaseOrderItemSeeder {
    fn name(&self) -> &'static str {
        "SeedPurchaseOrderItemSeeder"
    }

    fn order(&self) -> i32 {
        4
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if buying.purchase_order_items table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM buying.purchase_order_items")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/purchase_order_item_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM buying.purchase_order_items")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

