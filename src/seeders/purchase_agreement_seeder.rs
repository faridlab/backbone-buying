//! PurchaseAgreement seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial PurchaseAgreement data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PURCHASEAGREEMENT SEEDER
// ============================================================================

/// Seeder for purchase_agreement
pub struct SeedPurchaseAgreementSeeder;

impl SeedPurchaseAgreementSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPurchaseAgreementSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPurchaseAgreementSeeder {
    fn name(&self) -> &'static str {
        "SeedPurchaseAgreementSeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if buying.purchase_agreements table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM buying.purchase_agreements")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/purchase_agreement_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM buying.purchase_agreements")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
