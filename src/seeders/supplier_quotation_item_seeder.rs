//! SupplierQuotationItem seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial SupplierQuotationItem data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SUPPLIERQUOTATIONITEM SEEDER
// ============================================================================

/// Seeder for supplier_quotation_item
pub struct SeedSupplierQuotationItemSeeder;

impl SeedSupplierQuotationItemSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedSupplierQuotationItemSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedSupplierQuotationItemSeeder {
    fn name(&self) -> &'static str {
        "SeedSupplierQuotationItemSeeder"
    }

    fn order(&self) -> i32 {
        9
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if buying.supplier_quotation_items table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM buying.supplier_quotation_items")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/supplier_quotation_item_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM buying.supplier_quotation_items")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

