//! RfqSupplier seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial RfqSupplier data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// RFQSUPPLIER SEEDER
// ============================================================================

/// Seeder for rfq_supplier
pub struct SeedRfqSupplierSeeder;

impl SeedRfqSupplierSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRfqSupplierSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRfqSupplierSeeder {
    fn name(&self) -> &'static str {
        "SeedRfqSupplierSeeder"
    }

    fn order(&self) -> i32 {
        7
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if buying.rfq_suppliers table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM buying.rfq_suppliers")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/rfq_supplier_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM buying.rfq_suppliers")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

