//! Auth configuration for MaterialRequest handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<MaterialRequest>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::MaterialRequestService;
use crate::domain::entity::MaterialRequest;

/// Permission string constants for MaterialRequest operations.
pub mod permissions {
    pub const LIST:        &str = "material_request:list";
    pub const READ:        &str = "material_request:read";
    pub const CREATE:      &str = "material_request:create";
    pub const UPDATE:      &str = "material_request:update";
    pub const DELETE:      &str = "material_request:delete";
    pub const RESTORE:     &str = "material_request:restore";
    pub const TRASH:       &str = "material_request:trash";
    pub const EMPTY_TRASH: &str = "material_request:empty_trash";
    pub const BULK_CREATE: &str = "material_request:bulk_create";
    pub const UPSERT:      &str = "material_request:upsert";
}

/// Resource policy for MaterialRequest — maps CRUD actions to permission strings.
pub struct MaterialRequestPolicy;

#[async_trait]
impl ResourcePolicy<MaterialRequest> for MaterialRequestPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &MaterialRequest,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for MaterialRequest handlers.
///
/// Wraps `MaterialRequestService` and checks `MaterialRequestPolicy` before delegating.
pub type MaterialRequestGuard = PermissionGuard<MaterialRequest>;

/// Service-integrated permission guard for MaterialRequest (for use in generated handlers).
pub type MaterialRequestServiceGuard = ServicePermissionGuard<MaterialRequest, MaterialRequestService, MaterialRequestPolicy>;

// <<< CUSTOM
// END CUSTOM
