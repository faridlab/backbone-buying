//! Auth configuration for RfqItem handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<RfqItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::RfqItemService;
use crate::domain::entity::RfqItem;

/// Permission string constants for RfqItem operations.
pub mod permissions {
    pub const LIST:        &str = "rfq_item:list";
    pub const READ:        &str = "rfq_item:read";
    pub const CREATE:      &str = "rfq_item:create";
    pub const UPDATE:      &str = "rfq_item:update";
    pub const DELETE:      &str = "rfq_item:delete";
    pub const RESTORE:     &str = "rfq_item:restore";
    pub const TRASH:       &str = "rfq_item:trash";
    pub const EMPTY_TRASH: &str = "rfq_item:empty_trash";
    pub const BULK_CREATE: &str = "rfq_item:bulk_create";
    pub const UPSERT:      &str = "rfq_item:upsert";
}

/// Resource policy for RfqItem — maps CRUD actions to permission strings.
pub struct RfqItemPolicy;

#[async_trait]
impl ResourcePolicy<RfqItem> for RfqItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &RfqItem,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for RfqItem handlers.
///
/// Wraps `RfqItemService` and checks `RfqItemPolicy` before delegating.
pub type RfqItemGuard = PermissionGuard<RfqItem>;

/// Service-integrated permission guard for RfqItem (for use in generated handlers).
pub type RfqItemServiceGuard = ServicePermissionGuard<RfqItem, RfqItemService, RfqItemPolicy>;

// <<< CUSTOM
// END CUSTOM
