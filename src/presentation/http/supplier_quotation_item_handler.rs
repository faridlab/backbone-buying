//! SupplierQuotationItem REST handlers
//!
//! Auto-generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use rust_decimal::Decimal;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{SupplierQuotationItemService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateSupplierQuotationItemDto, UpdateSupplierQuotationItemDto, PatchSupplierQuotationItemDto, SupplierQuotationItemResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum SupplierQuotationItemError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for SupplierQuotationItemError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for SupplierQuotationItemError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "SUPPLIERQUOTATIONITEM_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "SUPPLIERQUOTATIONITEM_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "SUPPLIERQUOTATIONITEM_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "SUPPLIERQUOTATIONITEM_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for SupplierQuotationItem.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /supplier_quotation_items | List with pagination |
/// | POST | /supplier_quotation_items | Create new |
/// | GET | /supplier_quotation_items/:id | Get by ID |
/// | PUT | /supplier_quotation_items/:id | Full update |
/// | PATCH | /supplier_quotation_items/:id | Partial update |
/// | DELETE | /supplier_quotation_items/:id | Soft delete |
/// | POST | /supplier_quotation_items/bulk | Bulk create |
/// | POST | /supplier_quotation_items/upsert | Upsert |
/// | GET | /supplier_quotation_items/trash | List deleted |
/// | POST | /supplier_quotation_items/:id/restore | Restore |
/// | DELETE | /supplier_quotation_items/empty | Empty trash |
/// | GET | /supplier_quotation_items/:id/deleted | Get deleted by ID |
/// | DELETE | /supplier_quotation_items/trash/:id | Permanent delete from trash |
/// | GET | /supplier_quotation_items/count | Count active entities |
/// | GET | /supplier_quotation_items/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(SupplierQuotationItemService::with_repository(repository));
/// let router = create_supplier_quotation_item_routes(service);
/// ```
pub fn create_supplier_quotation_item_routes(service: Arc<SupplierQuotationItemService>) -> Router {
    BackboneCrudHandler::<SupplierQuotationItemService, SupplierQuotationItem, CreateSupplierQuotationItemDto, UpdateSupplierQuotationItemDto, SupplierQuotationItemResponseDto>::routes(
        service,
        "/supplier_quotation_items",
    )
}

/// Create Axum router with only the read (GET) endpoints for SupplierQuotationItem.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_supplier_quotation_item_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_supplier_quotation_item_read_routes(service: Arc<SupplierQuotationItemService>) -> Router {
    BackboneCrudHandler::<SupplierQuotationItemService, SupplierQuotationItem, CreateSupplierQuotationItemDto, UpdateSupplierQuotationItemDto, SupplierQuotationItemResponseDto>::read_routes(
        service,
        "/supplier_quotation_items",
    )
}

/// Create Axum router with only the write (mutation) endpoints for SupplierQuotationItem.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_supplier_quotation_item_write_routes(service: Arc<SupplierQuotationItemService>) -> Router {
    BackboneCrudHandler::<SupplierQuotationItemService, SupplierQuotationItem, CreateSupplierQuotationItemDto, UpdateSupplierQuotationItemDto, SupplierQuotationItemResponseDto>::write_routes(
        service,
        "/supplier_quotation_items",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_supplier_quotation_item_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<SupplierQuotationItemService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_supplier_quotation_item_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

