//! RfqSupplier API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the RfqSupplier CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// RFQSUPPLIER TEST DATA GENERATOR
// ============================================================================

/// Test data generator for RfqSupplier
pub struct RfqSupplierTestData;

impl TestDataGenerator for RfqSupplierTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "rfq_id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "supplier_id": Uuid::new_v4().to_string(),
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "rfq_id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "supplier_id": Uuid::new_v4().to_string(),
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/request_for_quotations", &super::request_for_quotation_api_test::RequestForQuotationTestData).await {
            deps.push(("rfq_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// RFQSUPPLIER API TEST
// ============================================================================

/// RfqSupplier API test suite
pub struct RfqSupplierApiTest {
    inner: GenericCrudTest<RfqSupplierTestData>,
}

impl RfqSupplierApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/rfq_suppliers", "RfqSupplier");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, RfqSupplierTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for RfqSupplierApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_rfq_supplier_crud() {
        let mut test = RfqSupplierApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add RfqSupplier specific tests here
// <<< CUSTOM TESTS END >>>
