//! PurchaseCompanySetting API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PurchaseCompanySetting CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PURCHASECOMPANYSETTING TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PurchaseCompanySetting
pub struct PurchaseCompanySettingTestData;

impl TestDataGenerator for PurchaseCompanySettingTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "double_validation": "one_step",
            "double_validation_amount": 0,
            "company_currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "send_reminder": false,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "double_validation": "one_step",
            "double_validation_amount": 0,
            "company_currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "send_reminder": false,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PURCHASECOMPANYSETTING API TEST
// ============================================================================

/// PurchaseCompanySetting API test suite
pub struct PurchaseCompanySettingApiTest {
    inner: GenericCrudTest<PurchaseCompanySettingTestData>,
}

impl PurchaseCompanySettingApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/purchase_company_settings", "PurchaseCompanySetting");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PurchaseCompanySettingTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PurchaseCompanySettingApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_purchase_company_setting_crud() {
        let mut test = PurchaseCompanySettingApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PurchaseCompanySetting specific tests here
// <<< CUSTOM TESTS END >>>
