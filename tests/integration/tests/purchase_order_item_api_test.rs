//! PurchaseOrderItem API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PurchaseOrderItem CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PURCHASEORDERITEM TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PurchaseOrderItem
pub struct PurchaseOrderItemTestData;

impl TestDataGenerator for PurchaseOrderItemTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "order_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "warehouse_id": null,
            "description": null,
            "quantity": 0,
            "rate": 0,
            "line_amount": 0,
            "received_qty": 0,
            "billed_qty": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "order_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "warehouse_id": null,
            "description": null,
            "quantity": 0,
            "rate": 0,
            "line_amount": 0,
            "received_qty": 0,
            "billed_qty": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/purchase_orders", &super::purchase_order_api_test::PurchaseOrderTestData).await {
            deps.push(("order_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// PURCHASEORDERITEM API TEST
// ============================================================================

/// PurchaseOrderItem API test suite
pub struct PurchaseOrderItemApiTest {
    inner: GenericCrudTest<PurchaseOrderItemTestData>,
}

impl PurchaseOrderItemApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/purchase_order_items", "PurchaseOrderItem");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PurchaseOrderItemTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PurchaseOrderItemApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_purchase_order_item_crud() {
        let mut test = PurchaseOrderItemApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PurchaseOrderItem specific tests here
// <<< CUSTOM TESTS END >>>
