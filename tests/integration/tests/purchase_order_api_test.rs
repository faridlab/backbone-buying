//! PurchaseOrder API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PurchaseOrder CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PURCHASEORDER TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PurchaseOrder
pub struct PurchaseOrderTestData;

impl TestDataGenerator for PurchaseOrderTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "po_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "supplier_quotation_id": null,
            "order_kind": "standard",
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "supplier_id": Uuid::new_v4().to_string(),
            "status": "draft",
            "order_date": Utc::now().format("%Y-%m-%d").to_string(),
            "schedule_date": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "subtotal": 0,
            "tax_rate": 0,
            "tax_amount": 0,
            "total": 0,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "po_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "supplier_quotation_id": null,
            "order_kind": "standard",
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "supplier_id": Uuid::new_v4().to_string(),
            "status": "draft",
            "order_date": Utc::now().format("%Y-%m-%d").to_string(),
            "schedule_date": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "subtotal": 0,
            "tax_rate": 0,
            "tax_amount": 0,
            "total": 0,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PURCHASEORDER API TEST
// ============================================================================

/// PurchaseOrder API test suite
pub struct PurchaseOrderApiTest {
    inner: GenericCrudTest<PurchaseOrderTestData>,
}

impl PurchaseOrderApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/purchase_orders", "PurchaseOrder");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PurchaseOrderTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PurchaseOrderApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_purchase_order_crud() {
        let mut test = PurchaseOrderApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PurchaseOrder specific tests here
// <<< CUSTOM TESTS END >>>
